const NEO_HOST_SUFFIX = "neo.space";

export function isAllowedNeoUrl(value) {
  try {
    const host = new URL(String(value)).hostname.toLowerCase();
    return host === NEO_HOST_SUFFIX || host.endsWith(`.${NEO_HOST_SUFFIX}`);
  } catch {
    return false;
  }
}

export function buildNeoObservationScript({ mailboxes, cursors, limit }) {
  const requested = [...new Set((mailboxes || []).map(value => String(value).toLowerCase()))];
  const boundedLimit = Math.min(25, Math.max(1, Number(limit) || 25));
  return `const requested=${JSON.stringify(requested)};const cursors=${JSON.stringify(cursors || {})};const max=${boundedLimit};const result={provider:'neo_browser',mailboxes:{},messages:[],tabsInspected:0,navigationPerformed:false,accountSelectionPerformed:false,messageOpeningPerformed:false,mailboxMutation:false};const chrome=Application('Google Chrome');function clean(v,n=6000){return String(v||'').replace(/\\u0000/g,'').slice(0,n)}function neo(raw){try{const h=new URL(String(raw)).hostname.toLowerCase();return h==='neo.space'||h.endsWith('.neo.space')}catch{return false}}if(!chrome.running())throw new Error('NEO_BROWSER_NOT_RUNNING');const identityScript='('+function(requested){const text=v=>String(v||'').replace(/\\u0000/g,'').trim().toLowerCase();const body=text(document.body?.innerText);const attrs=[...document.querySelectorAll('[aria-label],[title],[data-email],[data-account],[value]')].map(e=>[e.getAttribute('aria-label'),e.getAttribute('title'),e.getAttribute('data-email'),e.getAttribute('data-account'),e.getAttribute('value')].filter(Boolean).join(' ')).join(' ').toLowerCase();return requested.filter(identity=>body.includes(identity)||attrs.includes(identity))}.toString()+')('+JSON.stringify(requested)+')';const activateScript=identity=>'('+function(identity){const text=v=>String(v||'').replace(/\\u0000/g,'').trim().toLowerCase();const exact=e=>{const values=[e.getAttribute?.('data-email'),e.getAttribute?.('data-account'),e.getAttribute?.('aria-label'),e.getAttribute?.('title'),e.textContent].filter(Boolean).map(text);return values.some(v=>v===identity||v.includes(identity))};const candidates=[...document.querySelectorAll('[data-email],[data-account],[aria-label],[title],button,[role="button"],li')].filter(e=>e.getClientRects().length&&exact(e));for(const candidate of candidates){const control=candidate.closest('button,[role="button"],[data-email],[data-account],li');if(!control)continue;const boundary=control.closest('aside,nav,[role="navigation"],[class*="sidebar"],[class*="account"],[class*="mailbox"]');if(!boundary)continue;const controlText=text(control.textContent);const controlAttrs=text([control.getAttribute('data-email'),control.getAttribute('data-account'),control.getAttribute('aria-label'),control.getAttribute('title')].filter(Boolean).join(' '));if(!controlText.includes(identity)&&!controlAttrs.includes(identity))continue;control.click();return{selected:true,identity,messageRowsClicked:false,messageOpened:false}}return{selected:false,identity,error:'exact mailbox account control not found',messageRowsClicked:false,messageOpened:false}}.toString()+')('+JSON.stringify(identity)+')';const observeScript=identity=>'('+function(mailbox,cursors,max){const visible=e=>!!(e&&e.getClientRects().length);const text=(v,n=6000)=>String(v||'').replace(/\\u0000/g,'').trim().slice(0,n);const selectors=['[role="row"]','[data-message-id]','[data-thread-id]','[data-testid*="message"]','[data-testid*="mail-row"]'];const rows=[...new Set(selectors.flatMap(selector=>[...document.querySelectorAll(selector)]))].filter(visible);const messages=[],rejected=[];for(const row of rows){const rowText=text(row.innerText||row.textContent,6000);if(rowText.length<3)continue;const messageId=text(row.getAttribute('data-message-id')||row.getAttribute('data-id')||row.id,500);if(!messageId){rejected.push('missing immutable message id');continue}const time=row.querySelector('time'),rawTime=time?.getAttribute('datetime')||row.getAttribute('data-received-at')||row.getAttribute('data-date')||time?.textContent||'';const parsed=Date.parse(rawTime);if(!Number.isFinite(parsed)){rejected.push('missing immutable timestamp');continue}const timestamp=new Date(parsed).toISOString(),cursor=cursors[mailbox];if(cursor&&(timestamp<cursor.timestamp||(timestamp===cursor.timestamp&&messageId<=cursor.messageId)))continue;const subjectNode=row.querySelector('[data-subject],[class*="subject"],[aria-label*="Subject"]');const subject=text(subjectNode?.getAttribute('data-subject')||subjectNode?.textContent||rowText.split(/\\n+/)[0],1200);const addresses=[...rowText.matchAll(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\\.[A-Z]{2,}/ig)].map(match=>match[0].toLowerCase());messages.push({mailbox,messageId,threadId:text(row.getAttribute('data-thread-id')||messageId,500),timestamp,sender:addresses[0]||'',recipients:[],subject,content:rowText,attachments:[],sourceUrl:text(location.origin+location.pathname,1000)});if(messages.length>=max)break}messages.sort((a,b)=>a.timestamp.localeCompare(b.timestamp)||a.messageId.localeCompare(b.messageId));return{messages:messages.slice(0,max),rejected:[...new Set(rejected)].slice(0,20),readOnly:true,navigationPerformed:false,messageOpeningPerformed:false,mailboxMutation:false}}.toString()+')('+JSON.stringify(identity)+','+JSON.stringify(cursors)+','+JSON.stringify(max)+')';for(const win of chrome.windows()){for(const tab of win.tabs()){const url=String(tab.url()||'');if(!neo(url))continue;result.tabsInspected++;let identities=[];try{identities=JSON.parse(tab.execute({javascript:'JSON.stringify('+identityScript+')'}))}catch(error){continue}for(const identity of identities){if(result.mailboxes[identity]){result.mailboxes[identity]={connected:false,error:'ambiguous mailbox identity across multiple NEO tabs'};continue}let selected;try{selected=JSON.parse(tab.execute({javascript:'JSON.stringify('+activateScript(identity)+')'}))}catch(error){selected={selected:false,error:clean(error.message||error,500)}}if(!selected.selected||selected.messageRowsClicked||selected.messageOpened){result.mailboxes[identity]={connected:false,error:selected.error||'safe mailbox account selection failed'};continue}result.accountSelectionPerformed=true;delay(1);let observed;try{observed=JSON.parse(tab.execute({javascript:'JSON.stringify('+observeScript(identity)+')'}))}catch(error){observed={messages:[],error:clean(error.message||error,500)}}result.mailboxes[identity]={connected:!observed.error,provider:'neo_browser',readOnly:true,error:observed.error||null,rejected:observed.rejected||[]};for(const message of observed.messages||[])result.messages.push(message)}}}for(const identity of requested)if(!result.mailboxes[identity])result.mailboxes[identity]={connected:false,error:'exact mailbox identity not found in an authenticated NEO tab'};result.messages.sort((a,b)=>a.timestamp.localeCompare(b.timestamp)||a.messageId.localeCompare(b.messageId));result.messages=result.messages.slice(0,max);JSON.stringify(result);`;
}

export function validateNeoObservation(observed, mailboxes) {
  if (!observed || observed.provider !== "neo_browser" || observed.navigationPerformed !== false || observed.messageOpeningPerformed !== false || observed.mailboxMutation !== false) throw new Error("NEO_READ_ONLY_PROOF_FAILED");
  for (const mailbox of mailboxes) {
    const connection = observed.mailboxes?.[mailbox];
    if (!connection?.connected || connection.provider !== "neo_browser" || connection.readOnly !== true) throw new Error(`NEO_MAILBOX_IDENTITY_NOT_VERIFIED: ${mailbox}`);
  }
  return observed;
}
