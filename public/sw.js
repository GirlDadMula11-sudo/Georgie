const CACHE = "georgie-shell-v7";
const SHELL = ["/", "/styles.css", "/sierra.css", "/enrollment.css", "/device-auth-policy.js", "/device-auth.js", "/app.js", "/sierra-desk.js", "/handsfree.js", "/manifest.webmanifest", "/georgie-icon.svg", "/georgie-avatar.svg"];
self.addEventListener("install",event=>{event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(SHELL)).then(()=>self.skipWaiting()));});
self.addEventListener("activate",event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim()));});
self.addEventListener("fetch",event=>{if(event.request.method!=="GET")return;const url=new URL(event.request.url);if(url.origin!==self.location.origin||url.pathname.startsWith("/api/"))return;event.respondWith(fetch(event.request,{cache:"no-store"}).then(response=>{if(response.ok){const clone=response.clone();caches.open(CACHE).then(cache=>cache.put(event.request,clone));}return response;}).catch(()=>caches.match(event.request).then(cached=>cached||caches.match("/"))));});
self.addEventListener("push",event=>{let data={};try{data=event.data?.json()||{};}catch{data={body:event.data?.text()||"Georgie has an update."};}event.waitUntil(self.registration.showNotification(data.title||"Georgie",{body:data.body||"New maintenance update",icon:"/georgie-icon.svg",badge:"/georgie-icon.svg",tag:data.eventId||"georgie-update",data:{url:data.url||"/?inbox=1"}}));});
self.addEventListener("notificationclick",event=>{event.notification.close();event.waitUntil(clients.matchAll({type:"window",includeUncontrolled:true}).then(list=>{const existing=list[0];if(existing){existing.focus();existing.navigate(event.notification.data?.url||"/");return;}return clients.openWindow(event.notification.data?.url||"/");}));});
