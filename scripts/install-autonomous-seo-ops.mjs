import fs from "node:fs";

function patchFile(path, transform) {
  const before = fs.readFileSync(path,"utf8");
  const after = transform(before);
  if (after !== before) fs.writeFileSync(path,after);
  return after !== before;
}

const toolsChanged = patchFile("src/tools.js", source => {
  const importAnchor='import { validateBrowserWorkflow } from "./browser-workflow.js";';
  const imports=`${importAnchor}\nimport { scheduleObjective, listScheduledObjectives, objectiveWorkerStatus, runObjectiveWorkerCycle } from "./objective-worker.js";\nimport { seoIntegrationStatus, searchConsolePerformance, inspectSearchConsoleUrl, submitSitemap, ga4Report, crawlWebsite, pageSpeed, submitIndexNow, recordOrganicAttribution, getApplicationFunnel, recordSeoExperiment, readFundedOutcome, recordSeoEvidence, listSeoEvidence, syntheticConversionTest, websiteControlStatus } from "./integrations/seo-ops.js";`;
  if (!source.includes('from "./objective-worker.js"')) source = source.replace(importAnchor, imports);
  const anchor='defineTool({name:"system.status",description:"Inspect Georgie\'s live capability manifest, connection states, verification paths, platform constraints, and resource balance without exposing credentials.",risk:"read",async run(){return getCapabilityManifest()}});';
  if (!source.includes('name:"system.objective_worker"')) {
    const block=`defineTool({name:"system.objective_worker",description:"Read the durable autonomous objective worker status. The worker runs server-side, resumes checkpoints after restart, uses leases, and remains approval-aware.",risk:"read",async run(){return objectiveWorkerStatus()}});\ndefineTool({name:"system.objective_schedule",description:"Schedule a durable multi-step governed objective. Each step names an existing governed tool, optional semantic verification, delay, and approval requirement. Duplicate stable keys collapse to one objective.",risk:"low_risk_write",async run({userId,args}){return scheduleObjective(userId,args||{})}});\ndefineTool({name:"system.objective_list",description:"List durable autonomous objectives, checkpoints, next steps, leases, retries and evidence summaries.",risk:"read",async run({userId,args}){return listScheduledObjectives(userId,args||{})}});\ndefineTool({name:"system.objective_run_cycle",description:"Run one bounded autonomous objective cycle immediately. Normal production operation also runs this automatically in the server runtime.",risk:"low_risk_write",async run({userId}){return runObjectiveWorkerCycle(userId)}});\ndefineTool({name:"seo.status",description:"Read Georgie's SEO operating-surface configuration: Search Console, GA4, PageSpeed, IndexNow, Sierra attribution, synthetic tests, website repository and durable evidence ledger.",risk:"read",async run(){return {...seoIntegrationStatus(),websiteControl:websiteControlStatus()}}});\ndefineTool({name:"seo.search_console",description:"Read Google Search Console query/page performance for Sierra: clicks, impressions, CTR and average position. Read-only.",risk:"read",async run({args}){return searchConsolePerformance(args||{})}});\ndefineTool({name:"seo.url_inspection",description:"Run Google Search Console URL Inspection for one Sierra URL, including indexing/mobile/rich-result state. Read-only.",risk:"read",async run({args}){return inspectSearchConsoleUrl(args?.url)}});\ndefineTool({name:"seo.ga4",description:"Read GA4 landing-page, session, engagement and key-event performance for Sierra. Read-only and aggregate.",risk:"read",async run({args}){return ga4Report(args||{})}});\ndefineTool({name:"seo.crawl",description:"Crawl Sierra's website like a bounded technical SEO crawler: status, redirect chain, title, description, canonical, robots, headings, structured data and internal links.",risk:"read",async run({args}){return crawlWebsite(args||{})}});\ndefineTool({name:"seo.pagespeed",description:"Run PageSpeed/Lighthouse SEO, performance, accessibility, best-practice and Core Web Vitals diagnostics for one Sierra URL.",risk:"read",async run({args}){return pageSpeed(args?.url,{strategy:args?.strategy})}});\ndefineTool({name:"seo.sitemap_submit",description:"Submit Sierra's sitemap to Google Search Console. This is an indexing side effect and remains approval governed.",risk:"external_side_effect",async run({args}){return submitSitemap(args?.sitemapUrl)}});\ndefineTool({name:"seo.indexnow",description:"Submit changed Sierra URLs to IndexNow. External indexing side effect; requires approval.",risk:"external_side_effect",async run({args}){return submitIndexNow(args?.urls||[])}});\ndefineTool({name:"seo.record_attribution",description:"Record exactly-once organic funnel attribution into Sierra through the typed service RPC. Synthetic rows must be marked test_mode and are excluded from production learning.",risk:"low_risk_write",async run({args}){return recordOrganicAttribution(args||{})}});\ndefineTool({name:"seo.application_funnel",description:"Read aggregate organic application funnel stages from Sierra with synthetic records excluded.",risk:"read",async run({args}){return getApplicationFunnel(args||{})}});\ndefineTool({name:"seo.experiment_record",description:"Create or update a governed SEO experiment record with page, hypothesis, variant, status and metadata.",risk:"low_risk_write",async run({args}){return recordSeoExperiment(args||{})}});\ndefineTool({name:"seo.funded_outcomes",description:"Read aggregate funded outcomes and Sierra revenue attributed to organic traffic, grouped by industry, product and landing page without borrower PII.",risk:"read",async run({args}){return readFundedOutcome(args||{})}});\ndefineTool({name:"seo.evidence_record",description:"Write a durable SEO evidence-ledger entry containing change, old/new state, commit, deployment, test result, metric before/after and rollback path. Secret and PII-shaped fields are redacted.",risk:"low_risk_write",async run({userId,args}){return recordSeoEvidence(userId,args||{})}});\ndefineTool({name:"seo.evidence_list",description:"Read Georgie's durable SEO action/evidence ledger.",risk:"read",async run({userId,args}){return listSeoEvidence(userId,args||{})}});\ndefineTool({name:"seo.synthetic_conversion",description:"Run a synthetic Sierra conversion check. It must be explicitly acknowledged as synthetic/test by the intake endpoint or certification fails closed, preventing CapitalMatch learning contamination.",risk:"low_risk_write",async run({args}){return syntheticConversionTest(args||{})}});\n${anchor}`;
    source = source.replace(anchor, block);
  }
  return source;
});

const manifestChanged = patchFile("src/capability-manifest.js", source => {
  const importAnchor='import { masterCloserContract } from "./master-closer.js";';
  if(!source.includes('from "./integrations/seo-ops.js"')) source=source.replace(importAnchor,`${importAnchor}\nimport { seoIntegrationStatus, websiteControlStatus } from "./integrations/seo-ops.js";\nimport { objectiveWorkerStatus } from "./objective-worker.js";`);
  const anchor='    investmentIntelligence: investmentCapabilityContract(),';
  if(!source.includes('autonomousObjectiveWorker:')) source=source.replace(anchor,`    autonomousObjectiveWorker: objectiveWorkerStatus(),\n    seoOperations: {\n      ...seoIntegrationStatus(),\n      websiteControl: websiteControlStatus(),\n      capabilities: ["search_console_performance","url_inspection","ga4_reporting","technical_crawl","pagespeed_lighthouse","core_web_vitals","sitemap_submission","indexnow","organic_attribution","application_funnel","seo_experiments","funded_outcome_aggregation","synthetic_conversion","durable_evidence_ledger"],\n      productionPromotionGoverned: true,\n      syntheticRecordsExcludedFromLearning: true,\n      borrowerPiiInSeoAnalytics: false\n    },\n${anchor}`);
  return source;
});

console.log(`[Georgie] autonomous SEO/objective tool surface installed: tools=${toolsChanged} manifest=${manifestChanged}`);
