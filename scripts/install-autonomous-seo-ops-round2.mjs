import fs from "node:fs";
function patch(path,fn){const a=fs.readFileSync(path,"utf8"),b=fn(a);if(a!==b)fs.writeFileSync(path,b);return a!==b;}
const tools=patch("src/tools.js",source=>{
  const anchor='import { validateBrowserWorkflow } from "./browser-workflow.js";';
  if(!source.includes('from "./seo-monitor.js"')) source=source.replace(anchor,`${anchor}\nimport { configureSeoMonitor, seoMonitorStatus, runSeoMonitorSchedulerCycle } from "./seo-monitor.js";\nimport { createSeoContentPipeline, checkpointSeoContentPipeline, seoContentPipelineStatus, seoContentPipelineContract } from "./seo-content-pipeline.js";\nimport { deploymentControlStatus, listVercelEnvironmentMetadata, promoteVercelDeployment, rollbackVercelDeployment, listRenderEnvironmentMetadata, redeployRender } from "./integrations/deployment-control.js";`);
  const statusAnchor='defineTool({name:"system.status",description:"Inspect Georgie\'s live capability manifest, connection states, verification paths, platform constraints, and resource balance without exposing credentials.",risk:"read",async run(){return getCapabilityManifest()}});';
  if(!source.includes('name:"seo.monitor.configure"')){
    const block=`defineTool({name:"seo.monitor.configure",description:"Enable, disable, or change Georgie's durable recurring SEO monitor. Minimum recurrence is hourly; the schedule survives restarts and creates governed objective-worker runs.",risk:"low_risk_write",async run({userId,args}){return configureSeoMonitor(userId,args||{})}});\ndefineTool({name:"seo.monitor.status",description:"Read the recurring SEO monitor schedule, next run, provider readiness and server scheduler state.",risk:"read",async run({userId}){return seoMonitorStatus(userId)}});\ndefineTool({name:"seo.monitor.run_cycle",description:"Run one recurring SEO scheduler decision immediately; it schedules a durable crawl/performance/funnel/indexing observation objective when due.",risk:"low_risk_write",async run({userId}){return runSeoMonitorSchedulerCycle(userId)}});\ndefineTool({name:"seo.content_pipeline.create",description:"Create a durable SEO content production pipeline with mandatory research, factual verification, Sierra insight, brief, draft, claims/compliance, linking/schema, staging, QA and governed publish stages.",risk:"low_risk_write",async run({userId,args}){return createSeoContentPipeline(userId,args||{})}});\ndefineTool({name:"seo.content_pipeline.checkpoint",description:"Advance one SEO content artifact by exactly one validated stage. Stages cannot be skipped and publish-ready must preserve explicit production approval.",risk:"low_risk_write",async run({userId,args}){return checkpointSeoContentPipeline(userId,args||{})}});\ndefineTool({name:"seo.content_pipeline.status",description:"Read current SEO content pipelines and stage artifacts. Secret-shaped fields are not accepted as authority.",risk:"read",async run({userId,args}){return seoContentPipelineStatus(userId,args||{})}});\ndefineTool({name:"deployment.status",description:"Read Georgie's governed Vercel/Render deployment-control capabilities without exposing environment values.",risk:"read",async run(){return deploymentControlStatus()}});\ndefineTool({name:"deployment.vercel.env_metadata",description:"List Vercel environment variable names, targets and metadata only; never returns values.",risk:"read",async run(){return listVercelEnvironmentMetadata()}});\ndefineTool({name:"deployment.render.env_metadata",description:"List Render environment variable names and metadata only; never returns values.",risk:"read",async run(){return listRenderEnvironmentMetadata()}});\ndefineTool({name:"deployment.vercel.promote",description:"Promote an existing Vercel preview deployment to production. Requires explicit approval and remains kill-switch governed.",risk:"external_side_effect",async run({args}){return promoteVercelDeployment({...args,approvalId:args?._governance?.approvalId||args?.approvalId})}});\ndefineTool({name:"deployment.vercel.rollback",description:"Point Vercel production traffic back to a specific previous deployment. Requires explicit approval and remains kill-switch governed.",risk:"external_side_effect",async run({args}){return rollbackVercelDeployment({...args,approvalId:args?._governance?.approvalId||args?.approvalId})}});\ndefineTool({name:"deployment.render.redeploy",description:"Trigger a Render redeploy of the configured Georgie service. Requires explicit approval. Historical rollback remains governed Git revert plus auto-deploy.",risk:"external_side_effect",async run({args}){return redeployRender({...args,approvalId:args?._governance?.approvalId||args?.approvalId})}});\n${statusAnchor}`;
    source=source.replace(statusAnchor,block);
  }
  return source;
});
const manifest=patch("src/capability-manifest.js",source=>{
  const importAnchor='import { masterCloserContract } from "./master-closer.js";';
  if(!source.includes('from "./seo-content-pipeline.js"')) source=source.replace(importAnchor,`${importAnchor}\nimport { seoContentPipelineContract } from "./seo-content-pipeline.js";\nimport { deploymentControlStatus } from "./integrations/deployment-control.js";`);
  const anchor='    investmentIntelligence: investmentCapabilityContract(),';
  if(!source.includes('seoContentProduction:')) source=source.replace(anchor,`    seoContentProduction: seoContentPipelineContract(),\n    deploymentControl: deploymentControlStatus(),\n${anchor}`);
  return source;
});
console.log(`[Georgie] autonomous SEO round2 installed: tools=${tools} manifest=${manifest}`);
await import("./install-seo-phase2-executor.mjs");
