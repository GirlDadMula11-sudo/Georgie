import fs from "node:fs";

function patch(path,replacements){let source=fs.readFileSync(path,"utf8"),changed=false;for(const [from,to,label] of replacements){if(source.includes(to))continue;if(!source.includes(from))throw new Error(`reliability round2 installer could not find ${label} in ${path}`);source=source.replace(from,to);changed=true;}if(changed)fs.writeFileSync(path,source);return changed;}

const georgieChanged=patch("src/georgie.js",[[`function reasoning(effort = "medium") { return { effort, context: "all_turns" }; }\nasync function jsonResponse({ model, instructions, input, effort = "medium" }) { return withModelPermit(async()=>{const response = await openAI("/responses", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ model, instructions, input, reasoning: reasoning(effort), text: { verbosity: "low" } }) }); const payload = await response.json(); const raw = extractResponseText(payload).trim(); return JSON.parse(raw.replace(/^\`\`\`json\\s*/i, "").replace(/\`\`\`$/i, "").trim());}); }`,`function reasoning(effort = "medium") { return { effort, context: "all_turns" }; }\nfunction parseModelJson(rawValue){\n  const raw=String(rawValue||"").trim().replace(/^\`\`\`json\\s*/i,"").replace(/\`\`\`$/i,"").trim();\n  try{return JSON.parse(raw);}catch{}\n  const firstObj=raw.indexOf("{"); const firstArr=raw.indexOf("[");\n  const start=firstObj<0?firstArr:firstArr<0?firstObj:Math.min(firstObj,firstArr);\n  if(start<0)throw new Error("Model did not return structured JSON");\n  const opener=raw[start],closer=opener==="{"?"}":"]"; let depth=0,inString=false,escaped=false;\n  for(let i=start;i<raw.length;i++){const ch=raw[i];if(inString){if(escaped)escaped=false;else if(ch==="\\\\")escaped=true;else if(ch==='"')inString=false;continue;}if(ch==='"'){inString=true;continue;}if(ch===opener)depth++;else if(ch===closer){depth--;if(depth===0)return JSON.parse(raw.slice(start,i+1));}}\n  throw new Error("Model returned incomplete structured JSON");\n}\nasync function jsonResponse({ model, instructions, input, effort = "medium" }) { return withModelPermit(async()=>{const response = await openAI("/responses", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ model, instructions, input, reasoning: reasoning(effort), text: { verbosity: "low" } }) }); const payload = await response.json(); return parseModelJson(extractResponseText(payload));}); }`,`planner JSON parser`]]);

const v2Changed=patch("src/v2-turn-engine.js",[[`import { investmentDirectResponse } from "./investment-intelligence.js";`,`import { investmentDirectResponse } from "./investment-intelligence.js";\nimport { reliabilityFastResponse } from "./reliability-fast-paths.js";`,`fast-path import`],[`export async function completeTurnV2({userId,sessionId,input,history=[],onProgress,shouldFinalize=()=>true}){const startedAt=Date.now();let firstResponseMs=0;`,`export async function completeTurnV2({userId,sessionId,input,history=[],onProgress,shouldFinalize=()=>true}){const startedAt=Date.now();let firstResponseMs=0;const reliabilityFast=reliabilityFastResponse(input);if(reliabilityFast){const latencyMs=Date.now()-startedAt;setImmediate(()=>Promise.all([appendSessionTurn({userId,sessionId,role:"user",content:input}),appendSessionTurn({userId,sessionId,role:"assistant",content:reliabilityFast.text})]).catch(()=>{}));return{...reliabilityFast,latencyMs,firstResponseMs:latencyMs,contextReadyMs:latencyMs,actions:[],evidence:[],evidenceFreshness:"not_required",confidence:"policy_backed"};}`,`early reliability fast path`]]);
console.log(`[Georgie] Reliability round2 installed: georgie=${georgieChanged} v2=${v2Changed}`);
